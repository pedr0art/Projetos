services:
  - type: web
    name: easy-csv-merge
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "streamlit run Easy_CSV_Merge.py --server.port $PORT --server.address 0.0.0.0"
